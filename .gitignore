# Secrets - never commit. Real values live only on this machine / in GitHub Actions secrets.
.env
.env.local

# Python virtual environment (2GB+) - recreate with: python -m venv venv && pip install -r requirements.txt
venv/
__pycache__/
*.pyc

# Regenerable build outputs - recreate with: python preprocess.py && python train.py
data/processed/
models/spam_detector.keras
models/gate_report.json
models/published_metrics.json

# Retrain-loop local state (cursor for incremental telemetry pulls)
data/raw/.last_telemetry_pull
data/raw/telemetry_export.csv
